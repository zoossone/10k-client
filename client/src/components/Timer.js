import React, { useState } from 'react';

const Timer = () => {
    const [time, setTime] = useState('')

    return (
        <div>
            Timer 페이지입니다.
        </div>
    );
};

export default Timer;